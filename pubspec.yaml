name: zitadel
version: 3.0.38
description: >-
  Dart package for ZITADEL relatet matters. Contains the compiled grpc service clients
  to access the ZITADEL API.

repository: https://github.com/smartive/zitadel-dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  fixnum: ^1.0.0
  grpc: ^3.0.0
  http: ^1.0.0
  jose: ^0.3.2
  protobuf: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  path: ^1.8.3
  test: ^1.21.6
